import * as React from "react";
import ClientList from "../../presentation/ClientList";

type ClienListContainerProps = {
  //
};

const ClienListContainer: React.FC<ClienListContainerProps> = () => {
  return <ClientList />;
};

export default ClienListContainer;
